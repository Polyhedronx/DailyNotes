## 1. `matches!` 宏

Rust 标准库中提供了一个非常实用的宏：`matches!`，它可以将一个表达式跟模式进行匹配，然后返回匹配的结果 `true` or `false`。例如，有一个动态数组，里面存有以下枚举：

```rust
enum MyEnum {
    Foo,
    Bar
}

fn main() {
    let v = vec![MyEnum::Foo,MyEnum::Bar,MyEnum::Foo];
}
```

现在如果想对 `v` 进行过滤，只保留类型是 `MyEnum::Foo` 的元素，可能会想这么写：

```rust
v.iter().filter(|x| x == MyEnum::Foo);
```

但是，实际上这行代码会报错，因为无法将 `x` 直接跟一个枚举成员进行比较。好在可以使用 `match` 来完成，但是会导致代码更为啰嗦，是否有更简洁的方式？答案是使用 `matches!`：

```rust
v.iter().filter(|x| matches!(x, MyEnum::Foo));
```

很简单也很简洁，再来看看更多的例子：

```rust
let foo = 'f';
assert!(matches!(foo, 'A'..='Z' | 'a'..='z'));

let bar = Some(4);
// 模式匹配：Some(x)；守卫语句：if x > 2
assert!(matches!(bar, Some(x) if x > 2));
```

**关键特性总结：**

1. `matches!` 只返回 `bool`
2. 支持：
   - 枚举解构
   - 区间模式
   - `|` 或模式
   - `if` 模式守卫
3. 不会移动值（除非模式本身触发 move）
4. 是语法糖，本质是 `match` 的布尔版本

---

## 2. 知乎评论反驳社会资源决定论

首先，对于数据。看你的论述你应该也知道社会学议题控制变量是很困难的吧？难怪如此讨巧地避而不谈自己的数据支撑有多薄弱，你的数据呢？我好歹有个人最新的生活经验，你继续用08年的固定视角来看问题吧。

其次，对于论据。我个人和我的同学生活学习了初中高中整整六年——其中从分班前很差很差的学生，到有高中重点班的吊车尾无不包含——如果只有一刻不停地盯着才算对于“大部分人在假努力”这一观点有价值的论据，那恐怕几乎所有社会学著作都不能称之为行之有效了。至少在我的观察样本中，策略选择差异比单纯刷题时长差异更能解释成绩分化，只听课和刷课内习题我不可能达到现在的成绩。

再次，对于观点的概念。我之所以说“最顶尖的教育资源”，也是我将网课和教辅的教学质量与我所在的中学所谓的顶级教师的教学质量相比较的结论，而对于现如今的学生来说，老师的答疑远不如免费的AI。

然后，关于语言风格。之所以使用“我认为”、“大部分”、“几乎不可能”这样的限定词只是我为了将我的观点和不可动摇的事实隔开，将其转化为一种社会观察的视角。而不像你一样自以为是，把自己的观点当作绝对不可能动摇的真理。

关于我是不是文科生。我是物化政选手，不是传统的文理分科。我的物理水平一直到大二也能当家教给高中生拔尖，数学水平中规中矩。

关于我为什么没上清北。我不想说所谓落榜之类的老套故事，因为即使是我发挥到最好也就是个华五水平，最后高考语文和英语没考好，为了计算机相关的专业选择了华南理工。如果我沉沦在教育资源就是不如北上广、但也比贵州青海要好的迷思里，那我一辈子可能就是一个二本、大专。但我知道现代社会有足以让人改变的资源，所以我不是二本大专，而是985计算机——就算不够好，也比自认倒霉自认不如人要好。

最后，关于要素。

要素之一是天赋：到底是谁说天赋不重要？我只是否认天赋决定论。不是没有天赋的人就可以自暴自弃了，远远没到你的天赋上限。

要素之二是方法，也即我所说的主观能动性：你有所谓“其他人都没有看网课的意识”的误解，实际上我说的是大部分人（至少在我的观察样本里，越差的学生越没有，而差劲的学生是很多的），而不是全部人。高中重点班看网课的风气盛行，到后期基本大家都会看，不过是方法不同、看的课程不同罢了。笔记、错题、网课和刷题训练这四者的权衡，是人人不同的，我所说的“假努力”也多发生在这里的一意孤行。

要素之三是招生资源：在北京和河南的录取率差异之下说这个要素不重要也是假的，但没办法，最终摊到个人，如果希望有所成就，要做的是改变自己而不是怨天尤人。

但所谓教育资源的差异，恕我依旧难以认同。我丝毫不觉得重点高中要比很烂的学区初中的环境好，同时课程好得有限、老师答疑水平远不如AI（虽然我当时是没有这种工具的，但现在的学生有了）、同学依旧有各种神人。甚至相比于很烂很松散的初中环境，高中对我的自学限制大很多。

最近在忙实习、项目，还要回老家过年，焦头烂额，没什么时间看知乎评论。快过年了，还是收收火气。你要是还不认可，就当我错了吧。

---

## 3. `@` 模式匹配

`@` 运算符允许为一个字段绑定另外一个变量。下面例子中，我们希望测试 `Message::Hello` 的 `id` 字段是否位于 `3..=7` 范围内，同时也希望能将其值绑定到 `id_variable` 变量中以便此分支中相关的代码可以使用它。我们可以将 `id_variable` 命名为 `id`，与字段同名，不过出于示例的目的这里选择了不同的名称。

```rust
enum Message {
    Hello { id: i32 },
}

fn main() {
    let msg = Message::Hello { id: 5 };

    match msg {
        Message::Hello {
            // 此处
            id: id_variable @ 3..=7,
        } => println!("id 值的范围在 [3, 7] 之间: {}", id_variable),
        Message::Hello { id: newid @ (10 | 11 | 12) } => {
            println!("id 值的范围在 [10, 12] 之间: {}", newid)
        }
        Message::Hello { id } => println!("Found some other id: {}", id),
    }
}
```

---

## 4. 可变引用的注意事项

使用模式 `&mut V` 去匹配一个可变引用时，需要格外小心，因为匹配出来的 `V` 是一个值，而不是可变引用。

```rust
fn main() {
    let mut v = String::from("hello,");
    let r = &mut v;

    match r {
        // 该变量的类型是 &mut String
       value => value.push_str(" world!") 
    }
}
```

---

## 5. 关联函数

这种定义在 `impl` 中且没有 `self` 的函数被称之为**关联函数**： 因为它没有 `self`，不能用 `f.read()` 的形式调用，因此它是一个函数而不是方法，它又在 `impl` 中，与结构体紧密关联，因此称为关联函数。

在之前的代码中，我们已经多次使用过关联函数，例如 `String::from`，用于创建一个动态字符串。

```rust
impl Rectangle {
    fn new(w: u32, h: u32) -> Rectangle {
        Rectangle { width: w, height: h }
    }
}
```

> Rust 中有一个约定俗成的规则，使用 `new` 来作为构造器的名称，出于设计上的考虑，Rust 特地没有用 `new` 作为关键字。

因为是函数，所以不能用 `.` 的方式来调用，我们需要用 `::` 来调用，例如 `let sq = Rectangle::new(3, 3);`。这个方法位于结构体的命名空间中：`::` 语法用于关联函数和模块创建的命名空间。